This example demonstrates how to post notifications from a viewmodel.

In a real application, notifications are posted **asynchronously**
when application state changes and these changes are considered "important"
to the user.

In the example, instead of using a real application state changes,
the notifications are triggered by an alarm.
